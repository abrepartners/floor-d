

## Update Luxury Vinyl (LVP) Image

Copy the uploaded image to replace the current vinyl flooring image asset. Since both `Products.tsx` and `FlooringCategory.tsx` import from `@/assets/flooring-vinyl.jpg`, the new image will propagate automatically.

### Steps
1. Copy `user-uploads://UVA50_05114_ROOM.jpg` to `src/assets/flooring-vinyl.jpg`

